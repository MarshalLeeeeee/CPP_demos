The two codes realize the calculation of the matrix multilication.

matrix_regular.cpp use single thread to implement the calculation

matrix_thread.cpp use multiple threads to implement the calculation
the demo uses as many threads as the row number
